---
id: w2/m6/t008
title: "CI and behavior coverage"
worker: worker1
status: todo
estimate: 45m
depends_on: [w2/m6/t006, w2/m6/t007]
---

## Objective

Validate user-visible behavior and failure modes against the actual supported hosts.

## Context

Why: Correct feasible omissions and misleading availability claims across the current adapter surface, with precise ownership for remaining blockers. See [milestone contract](README.md) and [feasibility research](../evidence/capability-feasibility.md).

Follow `.pm/DO_NOT_DO.md`: public APIs, host-offered choices, unchanged defaults, no TUI/private storage or silent replay. Research is evidence of feasibility, not delivery.

## Steps

1. Inspect the relevant public API, existing implementation and recorded probe evidence.
2. Implement or verify the objective; preserve independent native and adapter semantics.
3. Exercise the acceptance cases and record evidence before completion.

## Files

src/acp-agent.ts, src/muse-sdk.ts, src/muse-sdk-events.ts, src/muse-view-events.ts, src/modes.ts, src/config-options.ts, src/tests/, docs/

## Acceptance criteria

- [ ] Run npm run check, npm run build, npm run test:unit and required Muse loopback suites with MUSE_CODE_ACP_REQUIRE_MUSE=1. Verify pinned 1.1.1-R2514.1 and 1.2.1-R2847.1 separately; missing hosts are blockers, not passing skips. Add pack/standalone checks when affected. No paid providers.
- [ ] Provider requests or filesystem effects prove delivery; cover cancellation, stale events, baseline clients and isolated sessions. Record commands, versions and results.

## Out of scope

Unrelated feature work, private host APIs, paid-provider calls and release publication.
