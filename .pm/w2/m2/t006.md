---
id: w2/m2/t006
title: "CI and behavior coverage"
worker: worker1
status: todo
estimate: 45m
depends_on: [w2/m2/t004, w2/m2/t005]
---

## Objective

Run the repository's required checks and record the real-host result on 1.2.1 with every remaining failure accounted for.

## Context

- Required: `npm run check`, `npm run build`, `npm run test:unit`, and after build `MUSE_CODE_ACP_REQUIRE_MUSE=1 npm run test:muse-loopback`. Add `npm run test:pack-smoke` if entrypoints or packaging changed.
- The 2026-09-14 baseline on `1.2.1-R2847.1` was 22 passed and 6 failed of 28; compare against it directly.
- A remaining failure is acceptable only if it is one of this milestone's recorded blockers, named with its cause. Do not report a skipped check as passed.

## Steps

1. Run the required checks and fix failures.
2. Run the real-host suite and map every remaining failure to a recorded blocker; investigate anything unmapped.
3. Record commands, results, host version and unresolved limitations in the milestone README.

## Files

- `.pm/w2/m2/README.md`
- Any source or test file needed to fix failures

## Acceptance criteria

- [ ] All required checks pass, with no missing-host skip counted as success.
- [ ] Every remaining real-host failure maps to a recorded blocker in this milestone.
- [ ] Evidence names the host version and the delivered behavior for both causes.

## Out of scope

- Paid-provider integration tests; standalone packaging checks unless packaging changed.
