---
id: w2/m2/t005
title: "Simplify milestone changes"
worker: worker1
status: todo
estimate: 30m
depends_on: [w2/m2/t004]
---

## Objective

Review the milestone diff for unnecessary state, duplication and reusable logic, and apply behavior-preserving cleanup.

## Context

- Host-capability probing and failure-message construction are the likely duplication sites, alongside `src/muse-host.ts`'s existing probe and the compatibility metadata m1 added in `src/host-compatibility.ts`.
- Use the repository simplify skill if available; otherwise review directly.
- Cleanup must not change which prompts fail, which warnings are emitted, or any approval default.

## Steps

1. Review host probing, capability detection and diagnostic message construction for duplication.
2. Apply behavior-preserving improvements and rerun the affected tests.

## Files

- `src/muse-host.ts`, `src/host-compatibility.ts`, `src/mcp-overlay.ts`, `src/mcp-status.ts`, `src/muse-sdk-host.ts`

## Acceptance criteria

- [ ] Simplify findings are resolved or explicitly declined with a reason recorded here.
- [ ] No change to failing-prompt behavior, warnings or approval defaults.

## Out of scope

- New behavior or scope changes.
