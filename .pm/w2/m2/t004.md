---
id: w2/m2/t004
title: "Adoption surface: correct the supported-host statement and attributions"
worker: worker1
status: todo
estimate: 30m
depends_on: [w2/m2/t002, w2/m2/t003]
---

## Objective

Make the README, SDK migration notes and changelog state what the adapter actually does on the host a new install receives.

## Context

- `README.md` currently says Muse 1.2.1-R2847.1 is not supported, citing six failures, and attributes the `:auto-review` failure to legacy exec session continuation only. The `muse-sdk-live` failure contradicts that attribution.
- The launcher auto-updates and deletes superseded binaries, so the existing advice to pin 1.1.1 with `MUSE_CODE_EXECUTABLE` is not reachable for a fresh install; say so plainly whatever the outcome of t002 and t003.
- m1 already added a paragraph noting that one 1.2.1 defect is fixed; extend that rather than duplicating it.
- Do not claim general 1.2.1 support on the strength of these fixes alone; state exactly which cases were verified.

## Steps

1. Update the README compatibility section: the supported host, what works and what does not on 1.2.1, the corrected attribution, and the pinning caveat.
2. Update `docs/sdk-migration.md` observed-host-support material with the verified cases and any recorded blockers.
3. Add changelog entries under Unreleased for the delivered fixes and the recorded blockers.

## Files

- `README.md`, `docs/sdk-migration.md`, `docs/mcp-passthrough.md`, `CHANGELOG.md`

## Acceptance criteria

- [ ] The compatibility section names verified cases and recorded blockers separately, with no claim beyond the evidence.
- [ ] The `:auto-review` attribution matches t002's findings.
- [ ] The pinning advice reflects that the launcher removes superseded binaries.
- [ ] `npm run format:check` passes and links resolve.

## Out of scope

- Releasing to npm; that is a separate workflow.
