---
id: w2/m3/t005
title: "CI and behavior coverage"
worker: worker1
status: todo
estimate: 45m
depends_on: [w2/m3/t003, w2/m3/t004]
---

## Objective

Run the required checks and prove the presentation on a real host.

## Context

- Required: `npm run check`, `npm run build`, `npm run test:unit`, and after build `MUSE_CODE_ACP_REQUIRE_MUSE=1 npm run test:muse-loopback`.
- The real-host baseline carries the failures recorded in w2/m2; no new failure may appear beyond those.
- Capturing frames through a client that advertises no Muse extension is the evidence that matters here; the harness used on 2026-09-14 is retained under `artifacts/w2-m1-bex/` (gitignored).

## Steps

1. Run the required checks and fix failures.
2. Run the real-host suite and compare remaining failures against the w2/m2 baseline.
3. Capture and record the two rendered titles from a real two-stage run through a non-negotiating client.
4. Record commands, results, host version and limitations in the milestone README.

## Files

- `.pm/w2/m3/README.md`
- Any source or test file needed to fix failures

## Acceptance criteria

- [ ] All required checks pass, with no missing-host skip counted as success.
- [ ] No real-host failure appears beyond the blockers recorded in w2/m2.
- [ ] Evidence includes the actual rendered titles from a real staged run.

## Out of scope

- Paid-provider integration tests.
