---
id: w2/m3/t003
title: "Adoption surface: document staged permission presentation"
worker: worker1
status: todo
estimate: 30m
depends_on: [w2/m3/t001, w2/m3/t002]
---

## Objective

Describe what a user now sees when one command needs several approvals, and what a client gets with and without the negotiated extension.

## Context

- Surfaces: the README approval paragraph m1 added, the staged-approval section in `docs/sdk-migration.md`, the ADR003 permission rows, and `CHANGELOG.md`.
- Say plainly that the stage text comes from the host's own parse, so a client is not being shown an adapter guess about the command.
- Relate it to [003](../done/003.md) honestly: repeated prompts for one compound command are now distinguishable; repeated prompts for genuinely separate operations remain expected under Allow once.

## Steps

1. Update the README approval paragraph with what each prompt now shows.
2. Update the staged-approval section of `docs/sdk-migration.md` and the ADR003 permission rows.
3. Add a changelog entry under Unreleased.

## Files

- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`, `CHANGELOG.md`

## Acceptance criteria

- [ ] Docs state what is rendered, where it comes from, and what differs for a negotiating client.
- [ ] The relationship to 003 is stated without reopening it.
- [ ] `npm run format:check` passes and links resolve.

## Out of scope

- Editor UI this repository does not own.
