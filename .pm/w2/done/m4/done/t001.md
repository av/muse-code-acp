---
id: w2/m4/t001
title: "Define verified native and adapter approval policies"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t008]
---

## Objective

Turn the two-host probes into a precise policy contract for explicit automatic approval, rejection and prompted operation.

## Context

Why: Implement explicit automatic approval/rejection and independently selected sandbox posture without weakening defaults. See [milestone contract](../README.md) and [feasibility research](../../../evidence/capability-feasibility.md).

Follow `.pm/DO_NOT_DO.md`: public APIs, host-offered choices, unchanged defaults, no TUI/private storage or silent replay. Research is evidence of feasibility, not delivery.

## Steps

1. Inspect the relevant public API, existing implementation and recorded probe evidence.
2. Implement or verify the objective; preserve independent native and adapter semantics.
3. Exercise the acceptance cases and record evidence before completion.

## Files

src/acp-agent.ts, src/modes.ts, src/config-options.ts, src/muse-sdk.ts, src/muse-sdk-host.ts, src/muse-permissions.ts, src/session-preferences.ts, src/host-configuration.ts, src/tests/

## Acceptance criteria

- [x] Repeat native onRequest/promptUnmatched/denyUnmatched/allowAll with matched and unmatched effects; distinguish temporary requests from terminal policy resolution.
- [x] Define automatic client once-decisions separately from native policy and sandbox posture. Keep default onRequest; all broader choices opt-in.
- [x] Reproduce 010 through direct set_mode and set_config_option before any prompt. Preserve root/trust restrictions and document support matrix.

## Out of scope

Unrelated feature work, private host APIs, paid-provider calls and release publication.

## Completion evidence

Implemented and verified in the [milestone evidence](../evidence.md). Production SDK paths, isolated native probes, unit/wire tests and both host versions distinguish requested policy, effective policy, adapter decisions and sandbox effects. Final full-suite closeout is tracked separately by t010/t011.
