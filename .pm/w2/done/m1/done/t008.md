---
id: w2/m1/t008
title: "CI and behavior coverage"
worker: worker1
status: done
estimate: 45m
depends_on: [w2/m1/t006, w2/m1/t007]
---

## Objective

Run the repository's required checks and prove the delivered behavior and its failure modes on the supported host.

## Context

- Checks per the pm skill validation section: code changes require `npm run check`, `npm run build`, `npm run test:unit`; SDK/MSP, permissions and real-host claims additionally require `MUSE_CODE_ACP_REQUIRE_MUSE=1 npm run test:muse-loopback` after build with the supported host installed. `npm run test:pack-smoke` is required only if package entrypoints changed.
- Record host and SDK versions with results in the milestone's validation evidence.

## Steps

1. Run `npm run check`, `npm run build`, `npm run test:unit`.
2. Run `MUSE_CODE_ACP_REQUIRE_MUSE=1 npm run test:muse-loopback`, including the new multi-stage suite; fix failures.
3. Record commands, results, host version (`muse --version`) and unresolved limitations in `README.md` of this milestone.

## Files

- `.pm/w2/m1/README.md` (validation evidence)
- Any source or test file needed to fix failures

## Acceptance criteria

- [x] All required checks pass; no missing-host skip is counted as success.
- [x] Evidence names the host version and covers multi-stage allow, deny at stage 2, the watchdog and the exhaustiveness test.

## Out of scope

- Paid-provider integration tests; standalone packaging checks unless packaging changed.

## Evidence

Delivered: check, build, test:unit (369/58), test:pack-smoke all pass. `MUSE_CODE_ACP_REQUIRE_MUSE=1 npm run test:muse-loopback` on 1.2.1 is 22 passed / 6 failed, and those 6 are byte-identical to the stashed-baseline measurement at `852a612` (pre-existing, README-documented 1.2.1 defects).
