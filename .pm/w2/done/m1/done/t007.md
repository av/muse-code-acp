---
id: w2/m1/t007
title: "Simplify milestone changes"
worker: worker1
status: done
estimate: 30m
depends_on: [w2/m1/t006]
---

## Objective

Review the milestone diff for unnecessary state, duplication and reusable logic, and apply behavior-preserving cleanup.

## Context

- The reconciler and watchdog both live in the 25 ms loop in `src/muse-sdk.ts`; approvals and user inputs now share one pattern and should share code where that is not forced.
- Use the repository's simplify skill if available; otherwise review directly.
- Read `.pm/DO_NOT_DO.md`; cleanup must not change permission gating or advertised behavior.

## Steps

1. Review pending-entity tracking, decision keys, watchdog bookkeeping and fixture script plumbing for duplication.
2. Apply behavior-preserving improvements and rerun the affected unit tests.

## Files

- `src/muse-sdk.ts`, `src/muse-permissions.ts`, `src/muse-view-events.ts`
- `src/tests/fixtures/fake-msp.cjs`, `src/tests/permissions.test.ts`

## Acceptance criteria

- [x] Simplify findings are resolved or explicitly declined with a reason recorded here.
- [x] No change to which choices are offered, when decides are sent, or when the watchdog fires.

## Out of scope

- New behavior or scope changes.

## Evidence

Delivered: factored `turnApprovals()` for the single fold read and moved the stall detail and signature builders into `muse-permissions.ts` as `approvalStallDetail` / `approvalSignature`. No behavior change; unit suite unchanged at green.
