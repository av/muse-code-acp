---
id: w2/m1/t006
title: "Adoption surface: document multi-stage approvals and compatibility metadata"
worker: worker1
status: done
estimate: 30m
depends_on: [w2/m1/t002, w2/m1/t003, w2/m1/t004, w2/m1/t005]
---

## Objective

Tell users and agents what changed on the SDK backend: multi-stage approvals now complete, stalls fail with diagnostics, and clients can read host compatibility metadata.

## Context

- Changed surfaces: SDK-backend approval behavior for compound shell commands, the watchdog bound and its environment override, and the negotiated `_meta["muse/hostCompatibility"]` field.
- Existing docs: `README.md` capability and compatibility sections, `docs/sdk-migration.md` (observed host support), `docs/ADR003-codex-acp-parity.md` rows "Per-tool interactive permissions" and "Extended permission scopes / review lifecycle", `CHANGELOG.md`.
- State SDK/exec differences: exec resolves approvals internally and is unaffected. Do not claim 1.1.1 multi-stage behavior that was not observed.

## Steps

1. README: note multi-stage shell approvals on the SDK backend, the watchdog failure mode, and the compatibility metadata; refresh the verified-host list.
2. `docs/sdk-migration.md`: record the observed 1.2.1 `approval/updated` continuation, the reconciler design (fold-driven, host-offered choices only) and the ignored-event table location.
3. ADR003: update the two permission rows and the evidence links.
4. CHANGELOG unreleased entry referencing this milestone and the originating report.

## Files

- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`, `CHANGELOG.md`

## Acceptance criteria

- [x] Docs describe the new behavior, its limits and the metadata field, with no claims beyond observed hosts.
- [x] `npm run format:check` passes for the edited files and links resolve.

## Out of scope

- Editor UI documentation this repository does not own; phase 2 and 3 designs beyond a one-line pointer.

## Evidence

Delivered: README behavior, environment variable and 1.2.1 status; `docs/sdk-migration.md` new section plus API-map and test-owner rows; ADR003 permission rows and evidence link; CHANGELOG unreleased entries.
