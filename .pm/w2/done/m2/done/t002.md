---
id: w2/m2/t002
title: "Resolve or record the `:auto-review` session-composition failure"
worker: worker1
status: done
estimate: 60m
depends_on: [w2/m2/t001]
---

## Objective

Make session load and resume work on 1.2.1, or record why they cannot and correct the README's explanation of the failure.

## Context

- Three failures share one host error: `compose session permission profile: permission profile ':auto-review' cannot be used: the automated reviewer is unavailable on this host`.
- The adapter starts sessions with `approvalMode: "onRequest"` (`src/muse-sdk-host.ts`) and never names a permission profile, so t001's frame evidence decides whether this is adapter-addressable.
- One failing case (`muse-sdk-live`) is an SDK-created session, so the README's current legacy-exec-only attribution is incomplete regardless of the fix outcome.
- Resume already re-applies approval mode through `session/setApprovalMode`; whether an equivalent public surface exists for the profile is the open question.
- `.pm/DO_NOT_DO.md`: do not invent an MSP method, do not silently fall back to another backend, do not weaken approval defaults to make a session load.

## Steps

1. From t001's evidence, determine whether the adapter can avoid or clear `:auto-review` through a public field on start, resume, or `session/setApprovalMode`.
2. If it can: implement it, keeping `onRequest` and sandbox defaults unchanged, and make the three failing cases pass.
3. If it cannot: record a host blocker in the milestone README with the minimal reproduction and exact host response, and make the adapter fail with an actionable message that names the host limitation instead of surfacing a raw internal error.
4. Cover the outcome: a real-host case for a fix, or a deterministic fake-host case asserting the actionable message for a blocker.

## Files

- `src/muse-sdk-host.ts`, `src/muse-sdk.ts`, `src/muse-host.ts`
- `src/tests/acp-restart-live.test.ts`, `src/tests/muse-sdk-live.test.ts`
- `src/tests/` (deterministic coverage for the chosen outcome)
- `.pm/w2/m2/README.md`

## Acceptance criteria

- [x] The three `:auto-review` cases pass on `1.2.1-R2847.1`, or the milestone records a reproduction, the exact host response and why no public adapter fix exists.
- [x] If unresolved, a user sees a message naming the host limitation rather than `internal error: compose session permission profile`.
- [x] Approval mode stays `onRequest` and no sandbox default is weakened.

## Out of scope

- HTTP MCP diagnostics (t003); the README wording (t004).

## Completion evidence

Recorded the host limitation with exact response and public reproduction. muse-sdk-host.ts maps only the known MSP -32603 profile error to actionable guidance. muse-sdk-host.test.ts verifies one resume, no new session or turn, no policy mutation, and host cleanup. The three real-host legacy paths assert this diagnostic on exactly 1.2.1-R2847.1; SDK-created continuity remains required to pass.
