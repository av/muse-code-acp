---
id: w2/m3/t004
title: "Simplify milestone changes"
worker: worker1
status: done
estimate: 30m
depends_on: [w2/m3/t003]
---

## Objective

Review the milestone diff for duplication and unnecessary state, and apply behavior-preserving cleanup.

## Context

- `src/muse-permissions.ts` now holds several helpers that each locate the current stage: `currentApprovalView`, `approvalStallDetail`, `approvalSignature` and the new presentation helper. Look for one shared accessor.
- Use the repository simplify skill if available; otherwise review directly.
- Cleanup must not change any rendered value or any offered choice.

## Steps

1. Review the stage-lookup helpers for a single shared accessor.
2. Apply behavior-preserving improvements and rerun the affected tests.

## Files

- `src/muse-permissions.ts`, `src/muse-sdk.ts`

## Acceptance criteria

- [x] Simplify findings are resolved or explicitly declined with a reason recorded here.
- [x] No rendered title, option or `_meta` value changes.

## Out of scope

- New behavior or scope changes.

## Completion evidence

Reviewed the milestone diff and consolidated current-stage lookup for title and stall diagnostics, matching both approval ID and source index. No new mutable state, shell parsing, or resolution mapping. Kept metadata projection separate because it intentionally omits argv and serves both pending and resolved evidence. Meaningful coverage pins the unchanged metadata and options.
