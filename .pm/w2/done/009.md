# 009 — Release a new version after the existing w2 fixes

Why: npm users need a published version containing the completed w2 fixes, including the multi-stage approval hang fix and distinguishable staged permission prompts.

Append this release after the existing w2 queue. Wait for `w2/m3` (through closeout
`w2/m3/t006`) and inbox items `w2/006`, `w2/007` and `w2/008` to be resolved, including
any implementation work they are promoted into. Include the already completed fixes
from `w2/001`, `w2/002`, `w2/m1` and `w2/m2`.

Follow `.agents/skills/release/SKILL.md` to select the appropriate next version,
verify the release candidate, write release notes covering these fixes and any
remaining documented host limitations, and publish through GitHub Actions.

Acceptance: the release workflow succeeds, the new version is available on npm,
and the version, release link and verification evidence are recorded here before
archiving this item. Update the w2 index's unreleased-fix notes to reflect the
published version.

Source: user request on 2026-09-14 to append a release after all existing fixes.

## Release candidate

Selected 0.5.0 for new negotiated observations and ACP config-option capabilities.
001/002 and m1–m3 are shipped; 006 shipped in `41a1fcd`, 007 in `ca2fbdd`, and
008 in `d016257` with upstream issue #10. All prerequisite notes are archived.
CI, npm, installed-package smoke and GitHub release are now verified below.

## Completed — 0.5.0 (2026-09-14)

- Release commit: `ffb42f7f6bd58faa5a1a25853584a5a452b53d57`; annotated tag `v0.5.0`, reachable from origin/main.
- [Release workflow 34827668744](https://github.com/bex-co/muse-code-acp/actions/runs/34827668744): all jobs succeeded, including exact-tag resolution, reusable CI and npm publish with signed provenance.
- [npm 0.5.0](https://www.npmjs.com/package/@bex-co/muse-code-acp/v/0.5.0): registry metadata verified `version: 0.5.0`, `latest: 0.5.0`, and SHA-1 `dc2c8f938ea46d56c2e88fe788a0f18158638eb3`, matching the publish log. npm's post-publish processing briefly returned 404s; verification waited for the actual package to become installable.
- [GitHub release v0.5.0](https://github.com/bex-co/muse-code-acp/releases/tag/v0.5.0): published, neither draft nor prerelease.
- Local validation: formatting, lint, build, all 377 unit tests and packed-install smoke passed. Runtime source is unchanged from 006's full 29-test loopback pass on both Muse 1.1.1-R2514.1 and 1.2.1-R2847.1.
- Release CI: 368 deterministic tests passed; 9 host-dependent tests skipped in the build job without Muse. All 29 required Linux loopback tests passed in the separate pinned-host job. macOS ARM64 standalone smoke passed, including operation without Node/Bun on PATH and a real-host prompt.
- Published-package smoke: installed the exact npm version into a fresh temporary directory, then exercised initialize/new/prompt/stream/end_turn/close with the isolated public-wire fixture. Verified exactly one turn/start carrying the requested text. The harness follows `scripts/pack-smoke.mjs` with registry installation replacing local packing; temporary installation was removed in finally.
- Release notes cover slash commands, mode config options, staged approvals, stall diagnostics, required MCP startup and observed session state. They retain the Muse 1.2.1 legacy `:auto-review` resume limitation and distinguish fixture policy-failure coverage from real-host observations.
- Cleanup: no release branch, PR or worktree created; temporary installation removed and named integration stashes restored and removed. The release tag is retained.

All prerequisites and release acceptance criteria are complete. The workstream
index now records publication; no pending w2 inbox note or milestone remains.
