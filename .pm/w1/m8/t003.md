---
id: w1/m8/t003
title: "Forward embedded text resources as attributed context"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m8/t002]
---

## Objective

Support editor selections, unsaved buffers and retrieved text sent as ACP embedded resources.

## Context

- Milestone goal: Forward editor-provided context faithfully and offer model settings verified against the Muse host.
- Why: Build on m7 content/workspace fixes and replace stale SDK-era assumptions before expanding capabilities.
- Reference context: `src/prompt-content.ts`, `src/config-options.ts`, `src/muse-host.ts`, `.tmp/codex-acp/src/CodexAcpClient.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Encode embedded text with URI/MIME attribution and unambiguous boundaries using m7 metadata conventions; preserve block ordering.
2. Advertise embedded context only for the supported contract; explicitly handle binary resources and size limits without fetching referenced URIs.

## Files

- `src/prompt-content.ts`
- `src/acp-agent.ts`
- `src/tests/prompt-content.test.ts`
- `src/tests/acp-wire.test.ts`

## Acceptance criteria

- [ ] Mixed text, embedded text, image and resource-link prompts preserve order in SDK input and supported exec input.
- [ ] Unsaved text reaches the model verbatim; malformed or unsupported binary content fails before host execution.
- [ ] Capability advertisement and tests match actual accepted content.

## Out of scope

- Editor fs proxying, remote URI fetching, and audio.
