---
id: w1/m8/t002
title: "Discover models and supported reasoning settings"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m8/t001]
---

## Objective

Replace the static SDK model menu with validated runtime discovery while retaining a documented compatibility fallback.

## Context

- Milestone goal: Forward editor-provided context faithfully and offer model settings verified against the Muse host.
- Why: Build on m7 content/workspace fixes and replace stale SDK-era assumptions before expanding capabilities.
- Reference context: `src/prompt-content.ts`, `src/config-options.ts`, `src/muse-host.ts`, `.tmp/codex-acp/src/CodexAcpClient.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Use the verified model/list contract and map host-supported model metadata and effort choices to ACP config options.
2. Bound and cache discovery by compatible host/config identity; preserve saved selections and handle unavailable or malformed discovery.

## Files

- `src/config-options.ts`
- `src/muse-sdk.ts`
- `src/muse-host.ts`
- `src/tests/config-options.test.ts`

## Acceptance criteria

- [ ] A fake host changing its model list changes advertised choices without a source edit.
- [ ] Unsupported discovery retains the configured default and explicit fallback; unsupported effort selections are rejected rather than falsely advertised.
- [ ] Reload and model-change behavior remain compatible with saved preferences.

## Out of scope

- Inventing provider models or changing credentials.
