---
id: w1/m17/t003
title: "Verify artifact lifecycle and client compatibility"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m17/t002]
---

## Objective

Prove artifacts remain usable for the promised lifetime and behave honestly after reload.

## Context

- Why: Prompt images and m13 file reports do not carry rich tool outputs; extend m9 translation with verified artifact access.
- Milestone goal: Preserve images, links and structured artifacts returned by tools instead of flattening all results to text.
- Reference context: `src/muse-sdk-events.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexToolCallMapper.ts`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Test output access while the host runs, after completion/cleanup, and on session load.
2. Document client capability behavior, artifact lifetime, truncation and unavailable references; add wire/loopback coverage.

## Files

- `src/tests/acp-wire.test.ts`
- `src/tests/session-history.test.ts`
- `docs/tool-results.md`

## Acceptance criteria

- [ ] Cleanup does not invalidate an artifact before its documented lifetime; expired references are explicitly unavailable.
- [ ] Wire tests cover supported rich output and baseline fallback, with bounded memory and no unsolicited URI fetching.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.
