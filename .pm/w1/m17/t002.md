---
id: w1/m17/t002
title: "Translate supported structured tool content"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m17/t001]
---

## Objective

Emit ordered ACP tool content for verified images, resource links and structured results with text fallback.

## Context

- Why: Prompt images and m13 file reports do not carry rich tool outputs; extend m9 translation with verified artifact access.
- Milestone goal: Preserve images, links and structured artifacts returned by tools instead of flattening all results to text.
- Reference context: `src/muse-sdk-events.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexToolCallMapper.ts`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Map public content fields without losing accompanying text or raw structured values.
2. Correlate items and revisions; bound decoding/retrieval and avoid duplicate artifacts at final snapshots.

## Files

- `src/muse-sdk-events.ts`
- `src/tool-calls.ts`
- `src/tests/muse-sdk-events.test.ts`

## Acceptance criteria

- [ ] A mixed text/image/link result remains ordered and visible with the original tool call ID.
- [ ] Completion-only results render once; malformed or unsupported content retains a useful fallback without failing unrelated output.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.
