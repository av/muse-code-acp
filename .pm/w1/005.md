# 005 — Future: Accessible native child sessions and isolated child controls

Why: retain the missing native capability for a future public SDK/host change without blocking current w2 delivery.

Estimate: 30m for one bounded reassessment when triggered; implementation requires
promotion back into w2 with appropriately sized tasks, not work hidden in this note.

## Evidence and current boundary

Fresh 1.2.1 produces workflow lifecycle events, so workflow visibility and exact workflow cancellation are delivered in w2/m9. Four observed reminder-child IDs still reject session/read and session/resume with -32020. Separate native child stream/history and permission isolation remain unproven.

The earlier `workflow/childControl` … invalid_target observation is withdrawn: that method is not in the public `MspMethod` union, so the rejection reflects an invented method, not host behavior.

**Reassessment on 1.3.0 (2026-09-15):** the budgeted bounded recheck is spent. The 1.1.1 launcher blocker is resolved — workflows launch and children complete — but the trigger is still not met. No `subagent` item is emitted for workflow children, so no public surface yields a `subagentId` and the nine declared `subagent/*` methods stay untargetable; observed `childSessionId` values still fail `session/read`, `view/page` and `session/resume` with -32020 while the root session succeeds as a control. [Full evidence](evidence/2026-09-15-w1-005-delegated-workers.md), public reproduction in `scripts/reproduce-delegated-workers.mjs`, and [upstream muse-code-sdk#13](https://github.com/meta-models/muse-code-sdk/issues/13), filed 2026-09-15 ([submitted text](evidence/2026-09-15-w1-005-upstream-issue-draft.md)). Do not spend another recheck without a new public artifact.

Source slices: w1/m11/t002, w1/m11/t003, w1/m11/t004, w1/m11/t005, w1/m15/t002.
Full original acceptance and [current/future allocation](../ownership-2026-09-14.md)
are preserved. This note is not a completed feature or a duplicate implementation task.

## Revisit trigger

A public host release exposes accessible child histories and stable routable native-child/control identities, or [muse-code-sdk#13](https://github.com/meta-models/muse-code-sdk/issues/13) resolves. Recheck by rerunning `scripts/reproduce-delegated-workers.mjs`.

Do not poll repeatedly without a new public artifact or concrete supported repro.
Uncertainty alone is not proof of impossibility; if a currently supported route is
found, immediately move that delivery scope into w2 and remove this duplicate watch.

## Acceptance after support appears

Verify child stream negotiation, restart history, distinct parent/sibling identity, real child approval grant/deny and targeted controls. Workflow handles and newly spawned root sessions are not substitutes.

Verify both public admission and user-visible outcome before advertising. Promote
implementation with adoption, simplification, required real-host CI and closeout;
keep blocked variants explicit. No private APIs, TUI automation or fake outcomes.
