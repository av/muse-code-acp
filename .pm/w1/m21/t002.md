---
id: w1/m21/t002
title: "Implement client-provided provider and gateway configuration"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m21/t001]
---

## Objective

Map verified ACP provider/gateway requests into isolated Muse session configuration.

## Context

- Why: Dynamic model selection does not cover ACP provider negotiation, service tiers, recommended choices or account quota; each needs independent evidence.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/model-discovery.ts`, `src/config-options.ts`, `src/auth.ts`, `src/host-configuration.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Validate endpoint/provider/auth configuration without logging secrets; bind discovery and host identity to the selected configuration.
2. Handle changed credentials, logout and host replacement; reuse existing auth semantics and coordinate m14 status output.

## Files

- `src/model-discovery.ts`
- `src/config-options.ts`
- `src/auth.ts`
- `src/host-configuration.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m21/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Two sessions with different local gateways cannot share credentials or cached model catalogs.
- [ ] A loopback turn reaches the intended endpoint; failures cannot silently fall back to a different provider.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
