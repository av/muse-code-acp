# Superseded w1/m11 source — 2026-09-14

Historical source, not an active task queue and not completed work. See [ownership ledger](../../../ownership-2026-09-14.md). All original requirements are allocated there.

## Original milestone

# w1 · m11 — Delegated worker lifecycle and native child sessions

**Worker:** worker1 **Goal:** Make delegated work visible, inspectable and controllable with correctly routed permissions. **Status:** todo (blocked: public host worker launch unavailable)

## Tasks (in order)

| id   | title                                                  | est | depends_on               |
| ---- | ------------------------------------------------------ | --- | ------------------------ |
| t001 | Render worker and workflow lifecycle cards             | 45m | w1/m10/t008              |
| t002 | Negotiate and route native ACP child sessions          | 45m | w1/m11/t001              |
| t003 | Restore child histories and worker state               | 45m | w1/m11/t002              |
| t004 | Route child approvals through the owning ACP client    | 45m | w1/m11/t003              |
| t005 | Expose verified worker controls and validate lifecycle | 45m | w1/m11/t004              |
| t006 | Simplify milestone changes                             | 30m | w1/m11/t005              |
| t007 | CI and behavior coverage                               | 45m | w1/m11/t005, w1/m11/t006 |
| t008 | Close out the milestone                                | 15m | w1/m11/t007              |

Estimated total: 5h 15m across 8 tasks. Priority: P1 worker visibility; progressive native integration. Scheduled after w1/m10/t008; cross-milestone dependencies refer to logical task IDs even after archival.

## Definition of done

- Start, progress, completion, failure and cancellation update the same worker card.
- Concurrent/nested children remain distinct; delegatedWorkers metadata reflects tested support.
- Negotiated clients see distinct child session streams; baseline clients continue to see cards.
- Repeated child updates do not create duplicate sessions or leak one parent's events into another.
- Load after restart restores available child history with stable identity and no duplicated messages.
- Unavailable history produces an explicit bounded response and does not execute a task.
- A child side effect waits for its corresponding approval and executes only after a valid host-offered grant.
- Denial, missing capability, malformed replies and stale decisions cannot authorize another child or turn.
- A control targets exactly one worker, with clear accepted/terminal results; sibling work remains unaffected.
- Real-host lifecycle evidence and wire tests cover both negotiated and legacy flows; unsupported required host APIs keep the affected implementation task open with a recorded blocker.
- /simplify and all required affected CI profiles pass, with validation evidence recorded before closeout.
- Required host support that cannot be verified leaves its delivery task open with the blocker documented; a schema declaration or an unsupported fallback alone does not satisfy delivery.
- SDK and legacy exec advertise only the behavior each implements; client-specific extensions require explicit negotiation and retain a documented baseline fallback.

## Source + Goal linkage

- **Source:** User request on 2026-09-12 to hand off all recommendations from the read-only muse-code-acp versus `.tmp/codex-acp` comparison to w1. Reference implementation paths: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`. The local reference checkout may be temporary; the objectives and acceptance criteria here preserve the handoff.
- **Goal linkage:** Make delegated work visible, inspectable and controllable with correctly routed permissions. This advances the project's goal of a reliable, faithful ACP adapter for Muse Code.
- **Expected outcome:** Provide useful worker visibility to every ACP client without requiring native child-session support. Offer separate child sessions only when both client and adapter support the selected extension. Make worker sessions inspectable after reload using public history surfaces. Ensure native and fallback worker flows preserve real Muse permission gating. Allow supported negotiated clients to target worker operations without affecting unrelated work.
- **Why now:** Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- **Sizing:** Multiple implementation and verification tasks exceed one hour; this is a shippable milestone rather than an inbox note.
- **Cross-surface parity:** Omitted because changes stay in this adapter's protocol/backend, tests and docs; there is no owned editor UI change. Protocol translation, client capability negotiation and SDK/exec differences remain explicit acceptance criteria.
- **Constraints:** Follow `.pm/DO_NOT_DO.md`: public SDK/MSP APIs and feature detection, exact SDK/minimum-host compatibility, real host-provided permission gating, sandbox defaults, no TUI automation and no silent ambiguous-turn replay. Adapt reference patterns without copying vendor-specific internals.

## Validation evidence

Blocked investigation on 2026-09-12 after shipped m10 (`3b26891`). m9 remains skipped under the earlier user instruction; no m11 task is marked complete.

- Environment: macOS aarch64, Muse Code 1.1.1-R2514.1 (host build b934305d214ca7b7ee5493ac696cd2109020b314), SDK 0.1.1, default durable `muse serve`, isolated dummy credentials and loopback provider; no paid provider calls.
- Reproduction: initialize, start a session, submit a turn; provider emits the host-offered `workflow` tool with `export default async function workflow(host) { return await host.agent({ input: "m11-child-probe: reply hello" }); }`. Wait for root completion and five seconds of background observation. Read root history and list sessions.
- The tool item's public `visibleOutput` claims `status: launched`, but the actual provider-facing `function_call_output` is `workflow_launch_unavailable: this client did not install a workflow launcher; no child work started`. A launch envelope alone is therefore not execution evidence. No workflow/subagent lifecycle items or verified worker control identity were emitted.
- Repeated with `initialize.capabilities.experimentalApi: true`; the host echoed true and produced the same launcher failure. A final independent default-capability run confirmed the exact failure again. `muse serve --help` exposes no workflow-launcher enablement option.
- Positive observation: real `reminderChild` items carry childSessionId values. However each returned child ID rejected both public `session/read` and `session/resume` with MSP `-32020`, session not found. Root read/list succeeded, so these are child lookup failures rather than connection failure. This does not establish readable/restorable child histories or routable child approvals.
- Public SDK declarations expose subagent controls targeting `(parent sessionId, subagentId)` and child history via childSessionId. Installed/reference generated method lists have no `view/subscribe`; a hand-authored transcript mentioning it is not a callable public contract. `MuseClient` ignores unknown-session notifications, and `Connection.onNotification` replaces its router rather than adding a second observer.
- Local reproducible probe and raw evidence are preserved under `.tmp/m11-probe/`: run `node .tmp/m11-probe/probe.mjs` or add `--experimental` after building the repo. This temporary directory is ignored and contains isolated probe data, not production implementation.
- Unblock requires a supported host/public integration with an installed worker launcher and accessible child sessions, followed by real child grant/deny, lifecycle, history and sibling-isolation tests. Alternatively, explicitly revise m11 scope to ship only verified baseline cards and defer native children/controls. No private storage parsing, fabricated transcripts or synthetic permission grants substitute for those requirements.

The investigation changed no production code. m11 remains open; committing this evidence and the parity planning documents does not ship its blocked implementation.

## Original t001

```markdown
---
id: w1/m11/t001
title: "Render worker and workflow lifecycle cards"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m10/t008]
---

## Objective

Provide useful worker visibility to every ACP client without requiring native child-session support.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Verify subagent/workflow/reminderChild items on the supported host and map identities, objectives, states, results and failure reasons to ordinary tool cards.
2. Maintain parent/child associations and distinguish child usage from parent usage; preserve generic fallback for unknown states.

## Files

- `src/muse-sdk-events.ts`
- `src/tests/muse-sdk-events.test.ts`
- `src/acp-agent.ts`

## Acceptance criteria

- [ ] Start, progress, completion, failure and cancellation update the same worker card.
- [ ] Concurrent/nested children remain distinct; delegatedWorkers metadata reflects tested support.

## Out of scope

- Native child-session advertisement before it is implemented.

## Blocker evidence (2026-09-12)

The real host's workflow tool returns `workflow_launch_unavailable: this client did not install a workflow launcher; no child work started` to the provider, even though the public tool item claims launch. Default and experimental initialization both reproduce it. Reminder-child IDs exist but public read/resume reject them with `-32020`. See the milestone validation evidence. Worker/workflow start-to-terminal delivery remains unverified, so this task and downstream native session/approval/control tasks remain open.
```

## Original t002

```markdown
---
id: w1/m11/t002
title: "Negotiate and route native ACP child sessions"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t001]
---

## Objective

Offer separate child sessions only when both client and adapter support the selected extension.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Document the extension version and feature-detect public Muse child identities/subscriptions.
2. Route child events to stable child ACP sessions while retaining root ownership and the legacy card fallback.

## Files

- `src/acp-agent.ts`
- `src/muse-sdk.ts`
- `src/tests/acp-wire.test.ts`
- `docs/subagent-sessions.md`

## Acceptance criteria

- [ ] Negotiated clients see distinct child session streams; baseline clients continue to see cards.
- [ ] Repeated child updates do not create duplicate sessions or leak one parent's events into another.

## Out of scope

- Editing external client UI or assuming a draft extension is universal ACP.
```

## Original t003

```markdown
---
id: w1/m11/t003
title: "Restore child histories and worker state"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t002]
---

## Objective

Make worker sessions inspectable after reload using public history surfaces.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Resolve child histories and provenance from verified SDK session/read/resume or retained supported helpers.
2. Reconstruct ordering and parent links without spawning fresh worker tasks; define missing/pruned history errors.

## Files

- `src/session-export.ts`
- `src/session-store.ts`
- `src/acp-agent.ts`
- `src/tests/session-history.test.ts`

## Acceptance criteria

- [ ] Load after restart restores available child history with stable identity and no duplicated messages.
- [ ] Unavailable history produces an explicit bounded response and does not execute a task.

## Out of scope

- Parsing undocumented worker storage or fabricating child transcripts.
```

## Original t004

```markdown
---
id: w1/m11/t004
title: "Route child approvals through the owning ACP client"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t003]
---

## Objective

Ensure native and fallback worker flows preserve real Muse permission gating.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Associate every approval with the verified parent/child/turn identity and send the host-offered choices through the root connection.
2. Invalidate pending decisions on child/root cancellation and close; test stale replies.

## Files

- `src/muse-sdk.ts`
- `src/muse-permissions.ts`
- `src/tests/permissions.test.ts`
- `src/tests/muse-sdk-approval-live.test.ts`

## Acceptance criteria

- [ ] A child side effect waits for its corresponding approval and executes only after a valid host-offered grant.
- [ ] Denial, missing capability, malformed replies and stale decisions cannot authorize another child or turn.

## Out of scope

- Synthetic grants or broadening permission scope.
```

## Original t005

```markdown
---
id: w1/m11/t005
title: "Expose verified worker controls and validate lifecycle"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t004]
---

## Objective

Allow supported negotiated clients to target worker operations without affecting unrelated work.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Verify public subagent sendMessage/followupTask/interrupt/stop semantics and advertise only the implemented subset.
2. Add targeted controls, completion-race handling and root-close cleanup; document unsupported host behavior.

## Files

- `src/acp-agent.ts`
- `src/muse-sdk.ts`
- `src/tests/acp-wire.test.ts`
- `docs/subagent-sessions.md`

## Acceptance criteria

- [ ] A control targets exactly one worker, with clear accepted/terminal results; sibling work remains unaffected.
- [ ] Real-host lifecycle evidence and wire tests cover both negotiated and legacy flows; unsupported required host APIs keep the affected implementation task open with a recorded blocker.

## Out of scope

- Unverified background terminal control methods or long-running goal orchestration.
```

## Original t006

```markdown
---
id: w1/m11/t006
title: "Simplify milestone changes"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m11/t005]
---

## Objective

Run /simplify over this milestone's implementation while preserving its verified behavior.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Review the milestone diff for shared abstractions, unnecessary state and duplication.
2. Apply behavior-preserving improvements and rerun affected checks.

## Files

- `src/muse-sdk-events.ts`
- `src/muse-sdk.ts`

## Acceptance criteria

- [ ] /simplify is completed and recorded in the milestone validation evidence.
- [ ] The feature contracts and fallback behavior remain unchanged.

## Out of scope

- Unrelated refactors and new features.
```

## Original t007

```markdown
---
id: w1/m11/t007
title: "CI and behavior coverage"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t005, w1/m11/t006]
---

## Objective

Run /ci for this adapter and verify meaningful coverage of the shipped behavior and failure modes.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Run /ci using this repository's build/check, deterministic unit, Muse loopback and package smoke profiles as applicable; do not substitute unrelated monorepo commands.
2. Fill meaningful behavior gaps, fix failures and record commands, host versions, outcomes and any external-provider acceptance limits in the milestone README.

## Files

- `package.json`
- `src/tests/`
- `README.md`
- `docs/sdk-migration.md`
- `.pm/w1/m11/README.md`

## Acceptance criteria

- [ ] All required affected CI profiles pass with recorded evidence; regression tests assert behavior rather than tautological coverage.
- [ ] Real-host claims are backed by host acceptance evidence, and unsupported capabilities remain unadvertised.

## Out of scope

- Paid-provider runs unless independently authorized; publishing or pushing merely to close the board.
```

## Original t008

```markdown
---
id: w1/m11/t008
title: "Close out the milestone"
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m11/t007]
---

## Objective

Close the milestone only after every preceding task is complete and its observable definition of done holds.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Review acceptance criteria and validation evidence; leave blocked implementation open rather than equating a support investigation with a delivered feature.
2. Use /pm done for completed tasks, setting status: done, moving tNNN.md files to done/ and marking table rows — **DONE**; run it on this task last.
3. Set milestone Status to done, move the whole directory to w1/done/m11/ and check its workstream checkbox; preserve logical task IDs.

## Files

- `.pm/w1/m11`
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] All other task acceptance criteria and milestone definition of done are met before closeout.
- [ ] Task frontmatter, DONE rows, milestone status and workstream checkbox agree; the milestone resides in w1/done/m11/.

## Out of scope

- Marking blocked or unimplemented work done, or changing other milestones.
```
