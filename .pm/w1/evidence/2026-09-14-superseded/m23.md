# Superseded w1/m23 source — 2026-09-14

Historical source, not an active task queue and not completed work. See [ownership ledger](../../../ownership-2026-09-14.md). All original requirements are allocated there.

## Original milestone

# w1 · m23 — Session commands and remaining editor presentation contracts

**Worker:** worker1 **Goal:** Provide /skills and /logout using existing authoritative adapter operations. Add /rename and live title updates backed by supported persistent metadata. Provide ACP deletion with semantics distinct from closing retained history. Close the client interoperability gap between Muse's exact-target extension and the reference steering contract where semantics can be preserved. Present verified web-search actions/results as useful structured tool cards. **Status:** todo

## Tasks (in order)

| id   | title                                                     | est | depends_on               |
| ---- | --------------------------------------------------------- | --- | ------------------------ |
| t001 | Add local skills and logout command handlers              | 45m | w1/m10/t008              |
| t002 | Implement verified session rename                         | 45m | w1/m23/t001              |
| t003 | Implement verified session deletion                       | 45m | w1/m23/t002              |
| t004 | Assess and implement compatible steering negotiation      | 45m | w1/m23/t003              |
| t005 | Improve search presentation and publish command contracts | 45m | w1/m23/t004              |
| t006 | Simplify milestone changes                                | 30m | w1/m23/t005              |
| t007 | CI and behavior coverage                                  | 45m | w1/m23/t005, w1/m23/t006 |
| t008 | Close out the milestone                                   | 15m | w1/m23/t007              |

Estimated total: 5h 15m across 8 tasks. Only listed dependencies are prerequisites; milestone numbering does not impose a dependency on all earlier work.

## Definition of done

- Wire tests prove handlers do not accidentally submit a model prompt.
- Logout response does not imply exported credentials were removed; command delivery respects close/dispose.
- New titles persist across adapter restart and appear consistently in list/load/live updates.
- Late fallback titles cannot overwrite a user rename; unavailable native rename is not falsely advertised as cross-client persistence.
- Successful deletion makes the target unavailable to subsequent load/list while preserving other sessions.
- No public deletion support leaves delivery open; direct undocumented store deletion and changing close semantics are excluded.
- Wire fixtures for both contracts show accepted requests target the intended turn and stale requests never start unintended work.
- Incompatible idle behavior is explicitly rejected or documented; an alias alone is not claimed as full compatibility.
- Search cards display known query/action information without parsing unsupported private payloads or inventing citations.
- Generic tools still render and the documentation distinguishes supported handlers, native behavior and adapter-owned behavior.
- Required host delivery has acceptance evidence; unsupported required functionality stays open with a concrete upstream blocker.
- Explicit assessment/proposal work may finish with a documented non-target or infeasibility decision, but does not count as delivered parity.
- Adoption docs, /simplify and required affected CI checks are complete before closeout.

## Source + Goal linkage

- **Source:** User handoff of [ADR003 parity work](../../../../docs/ADR003-codex-acp-parity.md) on 2026-09-12. The earlier m9–m19 handoffs retain their existing scope. See [handoff coverage](../../../w2/011.md).
- **Goal linkage:** Close the named remaining editor/runtime integration gaps while preserving truthful, reliable Muse ACP behavior.
- **Expected outcome:** Provide /skills and /logout using existing authoritative adapter operations. Add /rename and live title updates backed by supported persistent metadata. Provide ACP deletion with semantics distinct from closing retained history. Close the client interoperability gap between Muse's exact-target extension and the reference steering contract where semantics can be preserved. Present verified web-search actions/results as useful structured tool cards.
- **Why now:** Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- **Sizing:** 5 substantive implementation/investigation tasks exceed one hour without counting closing tasks.
- **Cross-surface parity:** Omitted: this is adapter/API, tests and documentation work with no owned editor UI. Protocol and backend differences are tested explicitly.
- **Adoption surface:** Covered by the last implementation task and CI acceptance: update README, applicable SDK/MCP support docs and ADR003; no duplicate closing task is needed.
- **Constraints:** Follow `.pm/DO_NOT_DO.md`; public SDK/MSP with feature detection, exact SDK/minimum-host evidence, real host-offered permission gating, sandbox defaults and no TUI automation or ambiguous-turn replay. Do not modify archived milestones or infer publication authorization.

## Validation evidence

Pending implementation. ADR003 is a source-based comparison; new host surfaces still require verification. No feature or task is marked delivered by this handoff.

## Blocker — public session deletion (2026-09-12)

Triage outcome: blocked before implementation; all tasks remain open. Pinned
SDK 0.1.1's public `MspMethod` registry in
`node_modules/@muse-code/sdk/dist/src/msp.d.ts` has start/resume/fork/list/read,
but no root-session deletion operation. Installed Muse 1.1.1-R2514.1's public
CLI help likewise exposes no session-delete command. `subagent/close` and
`view/unsubscribe` are not deletion of retained root-session history.

Required t003 explicitly forbids undocumented store deletion or changing close
semantics. An adapter-only tombstone could hide a session from one client but
would not verify the requested native deletion. No such substitute was added.
Resume when a documented public deletion operation with active-session behavior
is available, then prove list/load absence and preservation of other sessions.

Skills/logout, adapter-owned rename and search presentation remain wanted; they
are not claimed delivered by this triage. t004–t008 depend on deletion through
the explicit task chain, and no other milestone currently depends on m23.
Continue independent w1 work. This board-only evidence was checked for Markdown
formatting and repository-local references; no runtime validation is claimed.

## Original t001

```markdown
---
id: w1/m23/t001
title: "Add local skills and logout command handlers"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m10/t008]
---

## Objective

Provide /skills and /logout using existing authoritative adapter operations.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Reuse the shipped /mcp dispatcher and reserve built-in command names with explicit skill collision rules.
2. List discovered skills without a model turn; route logout through the existing logout operation and preserve environment-key caveats.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Wire tests prove handlers do not accidentally submit a model prompt.
- [ ] Logout response does not imply exported credentials were removed; command delivery respects close/dispose.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t002

```markdown
---
id: w1/m23/t002
title: "Implement verified session rename"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m23/t001]
---

## Objective

Add /rename and live title updates backed by supported persistent metadata.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Verify a public rename operation or an explicitly adapter-owned title store; document its scope rather than modifying native logs.
2. Integrate with m19 authoritative metadata rules when available and protect against stale generated/listed titles.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] New titles persist across adapter restart and appear consistently in list/load/live updates.
- [ ] Late fallback titles cannot overwrite a user rename; unavailable native rename is not falsely advertised as cross-client persistence.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t003

```markdown
---
id: w1/m23/t003
title: "Implement verified session deletion"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m23/t002]
---

## Objective

Provide ACP deletion with semantics distinct from closing retained history.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Verify public host deletion and active-session constraints before advertising support.
2. Serialize against active work/binding, remove only owned preferences after confirmed native success, and test error paths with isolated fixtures.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Successful deletion makes the target unavailable to subsequent load/list while preserving other sessions.
- [ ] No public deletion support leaves delivery open; direct undocumented store deletion and changing close semantics are excluded.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t004

```markdown
---
id: w1/m23/t004
title: "Assess and implement compatible steering negotiation"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m23/t003]
---

## Objective

Close the client interoperability gap between Muse's exact-target extension and the reference steering contract where semantics can be preserved.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Compare method/capability shapes and idle/active/race semantics at the pinned reference; document an explicit mapping.
2. Add a negotiated compatibility surface only if behavior is truthful, retaining exact-target safety and existing Muse clients.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Wire fixtures for both contracts show accepted requests target the intended turn and stale requests never start unintended work.
- [ ] Incompatible idle behavior is explicitly rejected or documented; an alias alone is not claimed as full compatibility.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t005

```markdown
---
id: w1/m23/t005
title: "Improve search presentation and publish command contracts"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m23/t004]
---

## Objective

Present verified web-search actions/results as useful structured tool cards.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Map available tool arguments, queries, locations and result links with a generic fallback; preserve raw outputs and tool IDs.
2. Test search/fetch variants and completion-only events; document all milestone commands, extension differences and ADR003 status.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Search cards display known query/action information without parsing unsupported private payloads or inventing citations.
- [ ] Generic tools still render and the documentation distinguishes supported handlers, native behavior and adapter-owned behavior.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t006

```markdown
---
id: w1/m23/t006
title: "Simplify milestone changes"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m23/t005]
---

## Objective

Run /simplify on the milestone diff, preserving the verified feature and failure contracts.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Review unnecessary state, duplicated translation and reusable command/configuration helpers.
2. Apply behavior-preserving cleanup and rerun affected checks.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Simplify findings are resolved and documented.
- [ ] Capabilities, permission gating and compatibility fallbacks remain intact.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t007

```markdown
---
id: w1/m23/t007
title: "CI and behavior coverage"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m23/t005, w1/m23/t006]
---

## Objective

Run /ci using this repository's scripts and prove the milestone's observable behavior.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Run npm run check, npm run build and npm run test:unit; run npm run test:pack-smoke for packaging/entrypoint changes.
2. For SDK/MSP, permission or host claims run MUSE_CODE_ACP_REQUIRE_MUSE=1 npm run test:muse-loopback after build, and add feature-specific host evidence when existing suites do not cover it.
3. Add meaningful success/failure tests, fix failures and record commands, versions and outcomes in the milestone README.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Required affected checks pass without treating missing-host skips as success.
- [ ] New host capability claims have real acceptance evidence, not only fake-host fixtures.
- [ ] README, relevant support docs and ADR003 agree with implemented behavior and explicit remaining gaps.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```

## Original t008

```markdown
---
id: w1/m23/t008
title: "Close out the milestone"
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m23/t007]
---

## Objective

Close only after preceding tasks and the observable definition of done are satisfied.

## Context

- Why: Close, metadata updates, skills and Muse-specific steering leave separate delete, rename, built-in command and client interoperability gaps.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `src/acp-agent.ts`, `src/skills.ts`, `src/steering-protocol.ts`, `src/tool-calls.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Audit acceptance criteria and validation evidence; leave required unsupported delivery open with an explicit blocker.
2. Use /pm done for completed tasks to set status: done, move tasks into done/ and mark rows — **DONE**; complete this task last.
3. Set milestone Status to done, move the entire milestone to w1/done/m23/ and check its w1 checkbox, preserving logical IDs.

## Files

- `src/acp-agent.ts`
- `src/skills.ts`
- `src/steering-protocol.ts`
- `src/tool-calls.ts`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m23/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] All required implementation and closing work is complete; investigation alone has not been substituted for required delivery.
- [ ] Task states, DONE rows, milestone state and workstream checkbox agree and the milestone is physically archived.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
```
