---
id: w1/m12/t002
title: "Preserve fork workspace and configuration provenance"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m12/t001]
---

## Objective

Bind forks safely to their source workspace and make inheritance explicit.

## Context

- Milestone goal: Let clients explore an alternative conversation without changing the source session.
- Why: Build branching on the verified session ownership, configuration and history contracts rather than copying native files.
- Reference context: `src/acp-agent.ts`, `src/session-preferences.ts`, `.tmp/codex-acp/src/SessionFork.ts`, `.tmp/codex-acp/src/CodexAcpServer.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Restore authoritative model plus appropriate saved effort; define mode reset and client MCP refresh using existing resume conventions.
2. Persist only adapter-owned preferences and retain parent/boundary metadata where supported; serialize fork against close/binding operations.

## Files

- `src/session-preferences.ts`
- `src/acp-agent.ts`
- `src/tests/session-binding.test.ts`
- `src/tests/session-resume.test.ts`

## Acceptance criteria

- [ ] Source and fork continue independently with correct workspace/model/effort and no shared mutable ACP state.
- [ ] In-flight close/disposal cannot resurrect a fork, and sandbox defaults are preserved.

## Out of scope

- Additional workspace roots or implicit permission inheritance.
