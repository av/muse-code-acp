# Revisit host-owned MCP status support

Why: m16 reports configured inventory and observed startup failures, but current host connection state and tool/resource counts still need a future public observation surface.

Estimate: 30m for one bounded API/host reassessment.

Source: [ADR003 follow-up research](../../docs/ADR003-followup-research.md#3-mcp-diagnostics-need-host-owned-observations), 2026-09-12. Archived m16 stays complete for its delivered subset.

## Trigger and next action

At the next SDK/host compatibility review, inspect published MSP methods/notifications and muse mcp --help for host-owned status/inventory. Revisit when a documented release introduces it. Current pinned MSP has no such method; current CLI help offers OAuth login/logout only.

## Acceptance

- Record versions and supported public observations or retain a dated blocker with a specific revisit condition.
- A ready status must describe Muse's resident connection. A separate diagnostic client's successful initialize/tools/list cannot establish host readiness.
- If a surface appears, promote delivery tasks for host/config generation identity, timestamps, unknown/stale state, tool/resource counts, auth changes, reconnect and real-host fixtures.
- Keep local /mcp inspection free of implicit connection probes/OAuth. An optional endpoint diagnostic would need a separately labelled contract.
- Do not invent counts, expose credentials, reopen archived history, or silently treat unavailable status as connected.
