---
id: w1/m24/t003
title: "Validate clean installation and document release options"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m24/t002]
---

## Objective

Make supported installation paths reviewable and document remaining distribution-policy choices.

## Context

- Why: npm installation works, but reference standalone binaries and bundled compatible runtime reduce setup friction; feasibility and release policy need explicit treatment.
- Source: `docs/ADR003-codex-acp-parity.md`; task scope is the uncovered ADR003 surface, not a duplicate of m9–m19.
- References: `package.json`, `.github/workflows/ci.yml`, `.tmp/codex-acp/package.json`, `.tmp/codex-acp/docs/RELEASES.md`.
- Public-host declarations are leads rather than proof. Follow `.pm/DO_NOT_DO.md`; record unsupported required delivery as blocked.

## Steps

1. Add clean-install validation and update README, compatibility docs and ADR003 for prerequisites, platforms and artifact verification.
2. Record whether bundled Muse, preview channels and registry publication are feasible/recommended; prepare any authorized local release plumbing without publishing or changing remote release settings.

## Files

- `package.json`
- `.github/workflows/ci.yml`
- `src/tests/` (feature-specific behavior and wire/host cases)
- `README.md`, `docs/sdk-migration.md`, `docs/ADR003-codex-acp-parity.md`
- `.pm/w1/m24/README.md` (validation evidence only through pm)

## Acceptance criteria

- [ ] Documented supported install paths pass real package smoke and applicable host acceptance.
- [ ] Preview/registry parity has an explicit evidence-backed proposal or non-target rationale; no publication or release-policy change is inferred from this PM handoff.

## Out of scope

- Unrelated features, external client UI changes, private Muse APIs, fabricated permission choices or unsupported parity claims.
- Existing m9–m19 implementation scope; integrate with completed work without treating its blocked milestones as implicitly complete.
- Remote publishing, release-policy changes and paid-provider tests without separate authorization.
