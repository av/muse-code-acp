---
id: w1/m19/t002
title: "Implement bounded session pagination and public discovery"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m19/t001]
---

## Objective

Return paginated sessions with stable documented cursor semantics and truthful workspace filtering.

## Context

- Why: Filesystem scans and first-prompt titles limit session navigation; align public discovery with m12 branch provenance and existing history guarantees.
- Milestone goal: Make large session histories discoverable through bounded public APIs and keep client titles and metadata current.
- Reference context: `src/session-store.ts`, `src/session-export.ts`, `src/acp-agent.ts`, `.tmp/codex-acp/src/SessionMetadata.ts`, `.tmp/codex-acp/src/TitleGenerator.ts`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Prefer verified public session/list; preserve an explicit compatibility fallback for older hosts.
2. Bound page size and history traversal; validate stale/malformed cursors and concurrent additions without promising snapshot guarantees the host lacks.

## Files

- `src/session-store.ts`
- `src/muse-sdk.ts`
- `src/acp-agent.ts`
- `src/tests/session-store.test.ts`

## Acceptance criteria

- [ ] A large fixture traverses all sessions under the documented ordering with bounded per-request results.
- [ ] Workspace filtering, source/fork sessions and malformed cursors behave consistently; listing does not acquire an unnecessary writer lease.
- [ ] Supported public listing avoids a full native-store scan on each request.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.
