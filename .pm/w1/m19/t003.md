---
id: w1/m19/t003
title: "Publish and restore authoritative session metadata"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m19/t002]
---

## Objective

Keep titles, recency and provenance consistent between session lists and live updates.

## Context

- Why: Filesystem scans and first-prompt titles limit session navigation; align public discovery with m12 branch provenance and existing history guarantees.
- Milestone goal: Make large session histories discoverable through bounded public APIs and keep client titles and metadata current.
- Reference context: `src/session-store.ts`, `src/session-export.ts`, `src/acp-agent.ts`, `.tmp/codex-acp/src/SessionMetadata.ts`, `.tmp/codex-acp/src/TitleGenerator.ts`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Prefer host-provided titles/metadata and publish observed session_info_update changes; use a deterministic first-prompt fallback only when no authoritative title exists.
2. Use verified public history where sufficient, preserving export compatibility for gaps; test rename/update races and restart.

## Files

- `src/acp-agent.ts`
- `src/session-store.ts`
- `src/session-export.ts`
- `src/tests/session-history.test.ts`
- `docs/sdk-migration.md`

## Acceptance criteria

- [ ] List/load/resume and live updates agree on known titles and provenance; fallback generation never overwrites a newer authoritative title.
- [ ] History remains complete under the verified API/fallback policy and metadata updates do not duplicate transcript events.
- [ ] No extra paid model call is introduced solely for title generation.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.
