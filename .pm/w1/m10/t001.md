---
id: w1/m10/t001
title: "Verify host reuse boundaries and steering contract"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m9/t009]
---

## Objective

Define a supported host/session ownership model and exact-target steering behavior before changing lifecycle code.

## Context

- Milestone goal: Allow corrections during active work and reduce repeated startup through safely scoped host reuse.
- Why: Steering needs access to the live session; host reuse must preserve the settings and approval isolation currently provided by per-turn processes.
- Reference context: `src/muse-sdk.ts`, `src/acp-agent.ts`, `src/mcp-overlay.ts`, `.tmp/codex-acp/src/SteeringQueue.ts`, `.tmp/codex-acp/src/CodexAcpServer.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Measure process launches and startup behavior across two turns with the loopback harness; verify public turn/steer acceptance and terminal behavior.
2. Specify session ownership, idle disposal, model/mode/MCP compatibility boundaries and safe host replacement; retain per-turn operation when reuse is incompatible.

## Files

- `src/tests/muse-sdk-live.test.ts`
- `docs/sdk-migration.md`

## Acceptance criteria

- [ ] An executable test demonstrates accepted steering into the intended active turn.
- [ ] Documented reuse rules account for spawn-time sandbox flags, settings overlays, writer leases and shutdown; schema presence alone is not treated as proof.

## Out of scope

- Global pooling of unrelated sessions or relaxing sandbox flags.
