---
id: w1/m10/t004
title: "Implement negotiated serialized steering"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m10/t003]
---

## Objective

Accept user corrections through a documented ACP steering extension backed by public turn/steer.

## Context

- Milestone goal: Allow corrections during active work and reduce repeated startup through safely scoped host reuse.
- Why: Steering needs access to the live session; host reuse must preserve the settings and approval isolation currently provided by per-turn processes.
- Reference context: `src/muse-sdk.ts`, `src/acp-agent.ts`, `src/mcp-overlay.ts`, `.tmp/codex-acp/src/SteeringQueue.ts`, `.tmp/codex-acp/src/CodexAcpServer.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Borrow the Codex queue design concept with per-session serialization, exact turn IDs and explicit capability negotiation.
2. Convert supported prompt content consistently; define idle-session behavior and distinguish steering acceptance from turn completion.

## Files

- `src/acp-agent.ts`
- `src/muse-sdk.ts`
- `src/prompt-content.ts`
- `src/tests/acp-wire.test.ts`

## Acceptance criteria

- [ ] Two concurrent steering requests reach the intended turn in deterministic order.
- [ ] Unnegotiated/unsupported clients retain baseline prompt behavior; failed or ambiguous steering is not silently replayed as a new turn.

## Out of scope

- Replacing cancellation semantics or inventing standard ACP methods.
