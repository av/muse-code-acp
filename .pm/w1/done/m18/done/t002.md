---
id: w1/m18/t002
title: "Publish persistent goal state through ACP"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m18/t001]
---

## Objective

Forward snapshots and changes with correct restore and clear behavior across prompt boundaries.

## Context

- Why: Goal snapshots are currently ignored and are distinct from m9 todo plans; m10 session-owned subscriptions support updates outside a turn.
- Milestone goal: Expose persistent objective and progress separately from whether a prompt is currently running.
- Reference context: `src/muse-sdk.ts`, `src/acp-agent.ts`, `.tmp/codex-acp/docs/goal-extension.md`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Publish documented session-info metadata using session-owned subscriptions and client-compatible negotiation.
2. Restore goal state on load/resume; preserve unknown statuses and authoritative progress without treating active goal as active prompt.

## Files

- `src/muse-sdk.ts`
- `src/acp-agent.ts`
- `src/tests/acp-wire.test.ts`

## Acceptance criteria

- [x] A goal can remain active after session/prompt finishes and receive subsequent observed updates.
- [x] Clear removes old state; reload and duplicate events do not resurrect stale objectives or block ordinary prompts.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.

## Completion evidence

Verified by the milestone validation evidence and docs/goal-extension.md. Implementation, behavior tests and required local checks passed; unavailable controls remain explicitly unadvertised.
