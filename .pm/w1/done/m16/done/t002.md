---
id: w1/m16/t002
title: "Map remote servers into isolated session configuration"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m16/t001]
---

## Objective

Support verified HTTP MCP servers alongside existing stdio servers without changing user settings.

## Context

- Why: The current overlay rejects non-command servers; broaden integrations after m10 makes host/config replacement explicit.
- Milestone goal: Connect client-provided remote MCP servers through supported Muse transports and explain connection failures.
- Reference context: `src/mcp-overlay.ts`, `src/skills.ts`, `.tmp/codex-acp/src/CodexAcpClient.ts`, `.tmp/codex-acp/src/CodexCommands.ts`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Validate URLs/headers and map only documented host fields into the private settings overlay.
2. Preserve merge precedence and session isolation; replace reused hosts when connection configuration changes.

## Files

- `src/mcp-overlay.ts`
- `src/acp-agent.ts`
- `src/tests/mcp.test.ts`

## Acceptance criteria

- [x] A local HTTP MCP tool is discoverable and callable through ACP on the supported host.
- [x] Stdio remains functional; concurrent sessions cannot leak headers or override another session's servers.
- [x] Malformed/unsupported transport is rejected before execution and secrets are omitted from diagnostics.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.
