---
id: w1/m10/t003
title: "Preserve settings, MCP and permission isolation during reuse"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m10/t002]
---

## Objective

Maintain current configuration and security semantics when a process survives a turn.

## Context

- Milestone goal: Allow corrections during active work and reduce repeated startup through safely scoped host reuse.
- Why: Steering needs access to the live session; host reuse must preserve the settings and approval isolation currently provided by per-turn processes.
- Reference context: `src/muse-sdk.ts`, `src/acp-agent.ts`, `src/mcp-overlay.ts`, `.tmp/codex-acp/src/SteeringQueue.ts`, `.tmp/codex-acp/src/CodexAcpServer.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Keep private overlays alive only while their host needs them; replace the host at an idle boundary for incompatible model/provider/mode/MCP changes.
2. Separate permission and elicitation lifetimes by turn generation, and isolate concurrent ACP sessions.

## Files

- `src/mcp-overlay.ts`
- `src/muse-sdk.ts`
- `src/muse-permissions.ts`
- `src/tests/permissions.test.ts`
- `src/tests/mcp.test.ts`

## Acceptance criteria

- [x] MCP credentials, model/effort and read-only flags never leak between sessions or become stale after a settings change.
- [x] Late replies cannot authorize another turn; replacement/close cleans overlays even after failure.

## Out of scope

- Permission persistence beyond host-offered scopes.
