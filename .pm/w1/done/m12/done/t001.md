---
id: w1/m12/t001
title: "Verify and expose ACP session fork"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m10/t008]
---

## Objective

Map ACP session/fork onto public Muse session/fork with validated source and cut boundaries.

## Context

- Milestone goal: Let clients explore an alternative conversation without changing the source session.
- Why: Build branching on the verified session ownership, configuration and history contracts rather than copying native files.
- Reference context: `src/acp-agent.ts`, `src/session-preferences.ts`, `.tmp/codex-acp/src/SessionFork.ts`, `.tmp/codex-acp/src/CodexAcpServer.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Probe supported host behavior for default and explicit completed-turn boundaries and negotiate/advertise only verified support.
2. Create a distinct ACP session from the returned identity; reject unknown, in-progress or invalid boundaries without touching source state.

## Files

- `src/acp-agent.ts`
- `src/muse-sdk.ts`
- `src/tests/acp-wire.test.ts`

## Acceptance criteria

- [x] A fork returns a distinct session with the requested prior context and leaves the original unchanged.
- [x] Unknown sessions and invalid boundaries return meaningful errors; older hosts do not advertise unsupported fork.

## Out of scope

- Session deletion, direct storage copying or fake fork semantics.

## Dependency review (2026-09-12)

Prerequisites corrected by the user-authorized parity research handoff. See
`docs/ADR003-followup-research.md` section 4. Acceptance criteria and host
blockers are unchanged; this is not completion evidence.

## Completion evidence

Verified by the milestone validation evidence, native fork/restart acceptance, binding and metadata tests, and session branching documentation.
