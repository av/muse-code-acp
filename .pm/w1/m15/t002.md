---
id: w1/m15/t002
title: "Track background tasks across prompt boundaries"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m15/t001, w1/m11/t008]
---

## Objective

Retain background task identity, output ownership and terminal state on the session host.

## Context

- Why: m9 live output and m11 workers do not represent background commands; reuse m10 ownership and m11 child identity routing.
- Milestone goal: Keep background commands visible beyond prompt completion and allow safe targeted stopping where the host supports it.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/docs/async-tasks.md`, `.tmp/codex-acp/src/async-tasks/CodexBackgroundTerminalTasks.ts`.
- Follow `.pm/DO_NOT_DO.md`. SDK declarations are investigation leads, not proof of real-host support; verify public contracts before advertising capability.

## Steps

1. Build a session-owned registry using authoritative item IDs and child-session namespaces.
2. Keep output on its originating tool card; reconcile missed terminal state only from supported evidence and handle host death/close.

## Files

- `src/muse-sdk.ts`
- `src/muse-sdk-events.ts`
- `src/tests/muse-sdk-events.test.ts`

## Acceptance criteria

- [ ] Prompt completion does not falsely complete a still-running command.
- [ ] Interleaved root/child commands remain distinct; completion, failure and unknown lost-host outcomes are honest and deduplicated.

## Out of scope

- Changes to external client UIs, private Muse protocols, fabricated provider state, or unverified control methods.
- Duplicating m8–m13 work, unrelated refactors, paid-provider usage unless independently authorized, and publishing or pushing as part of board maintenance.

## Dependency review (2026-09-12)

Prerequisites corrected by the user-authorized parity research handoff. See
`docs/ADR003-followup-research.md` section 4. Acceptance criteria and host
blockers are unchanged; this is not completion evidence.
