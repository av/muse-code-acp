---
id: w1/m11/t003
title: "Restore child histories and worker state"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t002]
---

## Objective

Make worker sessions inspectable after reload using public history surfaces.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Resolve child histories and provenance from verified SDK session/read/resume or retained supported helpers.
2. Reconstruct ordering and parent links without spawning fresh worker tasks; define missing/pruned history errors.

## Files

- `src/session-export.ts`
- `src/session-store.ts`
- `src/acp-agent.ts`
- `src/tests/session-history.test.ts`

## Acceptance criteria

- [ ] Load after restart restores available child history with stable identity and no duplicated messages.
- [ ] Unavailable history produces an explicit bounded response and does not execute a task.

## Out of scope

- Parsing undocumented worker storage or fabricating child transcripts.
