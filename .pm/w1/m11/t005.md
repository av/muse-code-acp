---
id: w1/m11/t005
title: "Expose verified worker controls and validate lifecycle"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m11/t004]
---

## Objective

Allow supported negotiated clients to target worker operations without affecting unrelated work.

## Context

- Milestone goal: Make delegated work visible, inspectable and controllable with correctly routed permissions.
- Why: Richer runtime reporting and host ownership provide the base for child histories and controls; deliver baseline visibility before native extensions.
- Reference context: `src/muse-sdk-events.ts`, `src/muse-sdk.ts`, `.tmp/codex-acp/src/subagents/AcpSubagents.ts`, `.tmp/codex-acp/src/subagents/CodexSubagentEventRouter.ts`.
- Follow `.pm/DO_NOT_DO.md`; verify public host behavior before advertising support. Reference schema presence alone is not host acceptance evidence.

## Steps

1. Verify public subagent sendMessage/followupTask/interrupt/stop semantics and advertise only the implemented subset.
2. Add targeted controls, completion-race handling and root-close cleanup; document unsupported host behavior.

## Files

- `src/acp-agent.ts`
- `src/muse-sdk.ts`
- `src/tests/acp-wire.test.ts`
- `docs/subagent-sessions.md`

## Acceptance criteria

- [ ] A control targets exactly one worker, with clear accepted/terminal results; sibling work remains unaffected.
- [ ] Real-host lifecycle evidence and wire tests cover both negotiated and legacy flows; unsupported required host APIs keep the affected implementation task open with a recorded blocker.

## Out of scope

- Unverified background terminal control methods or long-running goal orchestration.
