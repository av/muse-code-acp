# Revisit public goal control support

Why: m18 delivered observation, but set/pause/resume/clear still lack an open owner for checking newly available host support.

Estimate: 30m for one bounded upstream/host reassessment, not a full control implementation.

Source: [ADR003 follow-up research](../../docs/ADR003-followup-research.md#2-goal-controls-have-an-upstream-lead-not-a-verified-api), 2026-09-12. Archived m18 is unchanged.

## Trigger and next action

On the next SDK/host compatibility review, compare the published MSP method registry and host release evidence with pinned SDK 0.1.1 / Muse 1.1.1-R2514.1. Repeat when a public release introduces goal controls, not on an automatic timer.

Public hand-authored fixtures at mirror commit fbce769 include goal/set, goal/pause, goal/resume and goal/clear scenarios. Their presence is a lead only; the pinned method enum omits goal controls. Do not dispatch those methods from fixtures alone.

## Acceptance

- Record the checked schema/SDK/host versions and whether a documented control API exists.
- If absent, retain a dated upstream blocker and concrete revisit trigger; do not fabricate controls or mark the feature delivered.
- If present, promote to a delivery milestone covering admission vs completion, idempotency, wake/pause/clear behavior, busy-turn races, capability subsets and real-host tests; reuse m18 observation.
- Do not use ordinary prompt text, model tools or TUI scraping as guaranteed pause/clear operations.
- No external issue submission, model spending or production change is part of this note.

## Scheduling after cleanup

Future SDK/host watch only. On verified support, promote delivery into w2 and remove this watch after preserving its scope; do not create a parallel w1 implementation queue.
