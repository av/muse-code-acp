name: Conventional PRs

# Validate conventional titles when contributors choose to open a PR.
# Releases are dispatched explicitly and do not require a PR.
on:
  pull_request:
    branches: [main]
    # labeled/unlabeled so validation re-runs when `dependencies` is applied.
    types: [opened, reopened, edited, synchronize, labeled, unlabeled]

permissions: {}

concurrency:
  group: "conventional-prs-${{ github.event.pull_request.number }}"
  cancel-in-progress: true

jobs:
  validate:
    name: Validate PR title
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      pull-requests: read

    steps:
      # Config reference: https://github.com/amannn/action-semantic-pull-request#configuration
      - uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Keep optional PR titles consistent with repository commit subjects.
          types: |
            feat
            fix
            perf
            revert
            docs
            style
            chore
            refactor
            test
            build
            ci
          # Dependabot titles itself conventionally already; don't block bot PRs.
          ignoreLabels: |
            dependencies
